export enum authRoutes {
  LOGIN_ROUTE = '/api/v1/access/login/',
  CSRF_TOKEN_ROUTE = '/api/v1/access/csrf-token/',
}

export enum libraryRoutes {
  GET_PROMPTS = '/api/v1/prompt/get-prompt-list-private/?page=1',
}
