// TODO: Give matcher in middleware.ts the list of routes as a variable.
// Currently it is hardcoded as '/' which is not good practice

// routes for authenticated users only
export const protectedRoutes = [
    '/',
]
